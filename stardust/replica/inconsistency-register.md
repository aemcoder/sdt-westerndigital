# Inconsistency register — westerndigital.com replica

No entries — pure replica. Everything not listed here is frozen; any design
delta found by the gate is a defect, not an improvement.

Deferred candidates surfaced by the brand-review tension detectors (NOT entries — recorded
for the reviewer only; adopting any of them requires an explicit user decision):

- T-img-alt-empty — a large share of captured images carry empty alt (decorative header logo,
  lazy placeholders). Replicated verbatim.
- T-cta-vocab — "Learn More" / "Read More" / "Learn Why" / "Get the Full Story" coexist. Replicated verbatim.
- T-scale — ad-hoc type scale. Replicated verbatim.
